extends layout

block content
  if !user
    p Failed to confirm your account. The confirmation code seems invalid.
  else if user.confirmCode && resent
    p The confirmation email has been resent to your email address.
  else if user.confirmCode
    p
      | Your account is not yet confirmed. Click the button below to resend the
      | confirmation email.
      form(method='post')
        input#submit(type='submit', value='Resend Confirmation Email')
  else
    p Your account has been confirmed. You can now login.

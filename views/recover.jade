extends layout

block content
  if !error && sent
    p The recovery email has been sent to your email address.
  else
    form(method='post')
      div.form-item
        label(for='email') Email:
        input#email(type='email', name='email', value=email, required)
      div.form-item
        input#submit(type='submit', value='Recover')
